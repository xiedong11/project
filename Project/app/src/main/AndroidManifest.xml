<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MyViewActivity" />
        <activity android:name=".recyclerview.RecyclerViewActivity" />
        <activity android:name=".view.MoveImageView" />
        <activity android:name=".recyclerview.RecyclerView_2" />
        <activity android:name=".okhttp.OKHttpTest"></activity>
        <activity android:name=".base.test"/>
        <activity android:name=".eventbus.EventBusReceive"/>
        <activity android:name=".eventbus.EventBusSend"/>
        <activity android:name=".indicator_point.IndicatorActivity"/>
    </application>

</manifest>